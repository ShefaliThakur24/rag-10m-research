# Skills (accepted by reviewer)

One subdirectory per skill: `skills/rag10m-<slug>/SKILL.md`. Lanes and synth check this directory at the start of every batch via `git rebase main`.

Format follows the create-skill convention: YAML frontmatter with `name` and `description`, then body with the procedure.
