# Rejected approaches

One line per rejected idea: `<approach> - rejected because <one-line reason> (cite: <claim-id>)`. Synth populates from lanes' research as approaches are explicitly disqualified.

---

(empty)
