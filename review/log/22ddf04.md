# Review: 22ddf04 [synth] tick-3

**Decision:** PASS (chat-agent self-review under degraded-subagent fallback path).

**Diff size:** 6 files, +71 / -13. doc/01 §2,5,6,7 rewritten; doc/02 §rerank filled + §generate extended; appendix/concepts.md: 1 entry promoted from stub, 4 new entries; synth/claims.jsonl +13; synth/sources.jsonl +9; CHANGELOG.md +1 section.

**Clause-by-clause:**
- **1 (quote accuracy):** PASS. Every doc/ number traces to a claim ID present in synth/claims.jsonl (E-C-0001 through E-C-0008 for engineering, C-C-0001 through C-C-0005 for community); each claim cites a primary source URL (Anthropic engineering subdomain, Azure model catalog, Qdrant docs, Eugene Yan/Hamel/Tianpan blogs). No invented numbers.
- **2 (citation freshness):** PASS. All sources within 24mo window.
- **3 (prose style):** PASS. Each filled section has the contract structure (Recommendation → Rationale with regimes → Alternatives). No hedge words without a numeric or named-regime qualifier. Reverse audit on §6 Rerank: "latency budget tolerates +100-500ms" (regime), "queries are overwhelmingly trivial single-hop" (regime), "vendor-self-published and warrant in-domain replication" (confidence qualifier).
- **4 (tradeoff specificity):** PASS. doc/02 §rerank rows each name a regime; e.g. "latency budget < ~300ms hard" vs "latency budget tolerates +100-500ms"; "trivial single-hop lookups" vs "reasoning-heavy queries". §generate's "best-effort search UIs where refusal is worse than a partial answer" is an explicit regime where cite-or-refuse loses.
- **5 (audience fit):** PASS. Numbers-first throughout: 5.7%→2.9%/1.9%, 81.59% vs 48.80%, top-100 → top-N. No re-explanation of basic RAG. New concepts (cite-or-refuse, retriever-eval-antipattern, eval-driven-development) link from doc to appendix.
- **6 (internal consistency):** PASS. The Anthropic stack (-35%/-49%/-67%) is monotone and consistent. Cohere reasoning numbers (E-C-0004) and multilingual (E-C-0005) tell the same story. The cite-or-refuse contract in §7 is consistent with the production-derived-eval recommendation: both push the eval and guardrail to where the system fails, not where the generator fails. No contradictions with synth tick 2 numbers (chunk 512-token recommendation preserved; hybrid recommendation preserved and reinforced).
- **7 (image necessity):** N/A. No images this tick; the rerank stack is small enough to convey in prose+table without a diagram.
- **8 (schema compliance):** PASS. New synth/claims.jsonl rows match the existing schema (id/topic/claim/evidence/counter_evidence/confidence/applies_to_our_problem/applies_when/last_reviewed). Confidence calibrated: medium for Anthropic-stack numbers (vendor benchmark but mechanism plausible), medium for cite-or-refuse (practitioner consensus).

**Write isolation:** PASS. Synth touched only doc/, appendix/, synth/, CHANGELOG.md; no writes to lanes/ or review/.

**Skill proposals:** None this commit.

**Coverage:** doc/01 stages filled = 6 of 7 (§1 Ingest still _TBD_). Tradeoff tables: 4 of 5 populated.

**Outstanding gap:** Ingest stage. Document parsing strategy for 10M+ heterogeneous docs (Unstructured.io / vendor APIs / custom OCR / PDF-specific pipelines) was deferred by every lane and remains the largest known unknown.
